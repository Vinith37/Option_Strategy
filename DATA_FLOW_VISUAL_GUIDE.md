# 🔄 Data Flow Visual Guide - Options Strategy Builder

## Complete Request-Response Cycle

This document provides visual diagrams of how data flows through the application from user interaction to chart display.

---

## Architecture Layers

```
┌─────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                        │
│  (React Components - TopNav, Sidebar, StrategyDetailPanel)  │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND STATE LAYER                      │
│     (React Hooks - useState, useEffect, useMemo, etc.)      │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      API INTEGRATION LAYER                   │
│         (payoffApi.ts - Health Check, Fetch, Fallback)      │
└────────────────────────────┬────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
┌───────────────────────────┐   ┌──────────────────────────┐
│    BACKEND API SERVER     │   │  LOCAL CALCULATIONS      │
│  (Node.js + Express)      │   │  (Browser-side fallback) │
│  - /api/health            │   │  - calculateCoveredCall  │
│  - /api/calculate-payoff  │   │  - calculateBullCall     │
│  - /api/strategies        │   │  - calculateIronCondor   │
└───────────────────────────┘   └──────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│                    CALCULATION SERVICE                       │
│        (calculations.ts - Payoff computation logic)          │
└─────────────────────────────────────────────────────────────┘
```

---

## Flow 1: Application Startup

```
┌─────────┐
│  START  │
│   APP   │
└────┬────┘
     │
     ▼
┌─────────────────────────────┐
│  Load App.tsx Component     │
│  - Initialize state         │
│  - Render TopNav            │
│  - Render Sidebar           │
│  - Show "Select Strategy"   │
└─────────┬───────────────────┘
          │
          ▼
┌─────────────────────────────┐
│  API Layer: Health Check    │
│  GET /api/health            │
│  Timeout: 2 seconds         │
└─────────┬───────────────────┘
          │
     ┌────┴────┐
     │         │
     ▼         ▼
  ┌─────┐  ┌──────┐
  │ 200 │  │ FAIL │
  │ OK  │  │ ERR  │
  └──┬──┘  └───┬──┘
     │         │
     ▼         ▼
┌──────────┐  ┌────────────────┐
│ Backend  │  │ Local Calcs    │
│ Mode     │  │ Mode           │
└──────────┘  └────────────────┘
     │                 │
     └────────┬────────┘
              │
              ▼
     ┌────────────────┐
     │  App Ready     │
     │  User can      │
     │  interact      │
     └────────────────┘
```

---

## Flow 2: User Selects Strategy

```
┌──────────────────┐
│  User clicks:    │
│  "Covered Call"  │
│  in Sidebar      │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────┐
│  App.tsx:                        │
│  handleStrategySelect("1")       │
│  - setSelectedStrategyId("1")    │
│  - setShowMobileDetail(true)     │  ← Mobile: Show detail panel
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  StrategyDetailPanel renders     │
│  - Strategy: Covered Call        │
│  - Type: covered-call            │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Load strategyConfig:            │
│  - Parameters (strikes, etc.)    │
│  - Default values                │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Initialize State:               │
│  - params = defaults             │
│  - entryDate = today             │
│  - expiryDate = today + 30 days  │
│  - underlyingPrice = 18000       │
│  - priceRangePercent = 30        │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  useEffect triggered:            │
│  calculatePayoff()               │
└────────┬─────────────────────────┘
         │
         ▼
    (See Flow 3)
```

---

## Flow 3: Calculate Payoff (Backend Available)

```
┌────────────────────────────┐
│  calculatePayoff()         │
│  - setIsLoadingPayoff(true)│  ← Show loading spinner
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  fetchPayoffData(request)          │
│  Request:                          │
│  {                                 │
│    strategyType: "covered-call",   │
│    entryDate: "2025-12-26",        │
│    expiryDate: "2026-01-26",       │
│    parameters: {                   │
│      futuresPrice: "18000",        │
│      callStrike: "18500",          │
│      premium: "200",               │
│      ...                           │
│    },                              │
│    underlyingPrice: 18000,         │
│    priceRangePercent: 30           │
│  }                                 │
└──────────┬─────────────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Check Backend Health        │
│  await checkBackendAvail()   │
└──────────┬───────────────────┘
           │
      ┌────┴─────┐
      │          │
      ▼          ▼
  ┌────────┐  ┌──────────┐
  │ TRUE   │  │  FALSE   │
  └───┬────┘  └────┬─────┘
      │            │
      ▼            ▼
┌──────────────┐  ┌────────────────────┐
│ POST Request │  │ Local Calculation  │
│ to Backend   │  │ (See Flow 4)       │
└──────┬───────┘  └──────┬─────────────┘
       │                 │
       ▼                 │
┌─────────────────────────┐
│  fetch(                 │
│    '/api/calculate-     │
│     payoff',            │
│    {                    │
│      method: 'POST',    │
│      headers: {...},    │
│      body: JSON.        │
│        stringify(req)   │
│    }                    │
│  )                      │
└──────┬──────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Backend Controller:     │
│  payoffController.ts     │
│  - Validate request      │
│  - Extract params        │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Calculation Service:    │
│  calculations.ts         │
│  - Generate price points │
│  - Calculate P&L         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Return JSON Response:   │
│  [                       │
│    {price:12600,pnl:-..},│
│    {price:12820,pnl:-..},│
│    ...                   │
│    {price:23400,pnl:..}  │
│  ]                       │
└──────┬───────────────────┘
       │
       └─────────────┬──────┘
                     │
                     ▼
            ┌────────────────────┐
            │  Frontend receives │
            │  payoff data       │
            └────────┬───────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  setPayoffData(data)       │
            │  setIsLoadingPayoff(false) │  ← Hide spinner
            └────────┬───────────────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  Chart Re-renders          │
            │  - Recharts detects new    │
            │    data prop               │
            │  - Animates transition     │
            │  - Updates axis ranges     │
            └────────────────────────────┘
```

---

## Flow 4: Calculate Payoff (Backend Unavailable - Fallback)

```
┌────────────────────────────┐
│  Backend Health Check      │
│  Returns: FALSE (timeout)  │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Use Local Calculations        │
│  calculatePayoffLocally(req)   │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Switch on strategyType:       │
│  case "covered-call":          │
│    return calculateCoveredCall │
│           (parameters)         │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  calculateCoveredCall(params)          │
│  - Parse params (lot size, prices)     │
│  - minPrice = price × 0.70             │
│  - maxPrice = price × 1.30             │
│  - Generate 50 price points            │
│  - For each point:                     │
│    • Calculate futures P&L             │
│    • Calculate call P&L                │
│    • Sum to total P&L                  │
│  - Return array of {price, pnl}        │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Return to fetchPayoffData()   │
│  Same format as backend:       │
│  [                             │
│    { price: 12600, pnl: -... },│
│    { price: 12820, pnl: -... },│
│    ...                         │
│  ]                             │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  setPayoffData(data)           │
│  - Chart updates               │
│  - User sees no difference     │
│    (seamless fallback)         │
└────────────────────────────────┘
```

---

## Flow 5: User Changes Parameter (Live Update)

```
┌────────────────────────────┐
│  User types in input:      │
│  "Call Strike Price"       │
│  18500 → 19000             │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  onChange Handler:             │
│  handleParamChange(            │
│    "callStrike",               │
│    "19000"                     │
│  )                             │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Update State:                 │
│  setParams(prev => ({          │
│    ...prev,                    │
│    callStrike: "19000"         │
│  }))                           │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  useEffect Triggered:          │
│  useEffect(() => {             │
│    calculatePayoff()           │
│  }, [params])                  │
└──────────┬─────────────────────┘
           │
           ▼
   (Repeat Flow 3 or 4
    with new parameters)
           │
           ▼
┌────────────────────────────────┐
│  Chart Updates:                │
│  - New payoff curve            │
│  - New max profit/loss         │
│  - New break-even points       │
│  - Smooth animation (500ms)    │
└────────────────────────────────┘
```

---

## Flow 6: User Adjusts Price Range Slider

```
┌────────────────────────────┐
│  User drags slider:        │
│  Price Range: 30% → 50%    │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  onChange Handler:             │
│  setPriceRangePercent(50)      │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Debounce (300ms):             │
│  Wait for user to stop sliding │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  useEffect Triggered:          │
│  useEffect(() => {             │
│    calculatePayoff()           │
│  }, [priceRangePercent])       │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Recalculate Price Range:          │
│  minPrice = 18000 × (1 - 0.50)     │
│           = 9,000                  │
│  maxPrice = 18000 × (1 + 0.50)     │
│           = 27,000                 │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Generate New Price Points:        │
│  step = (27000 - 9000) / 49        │
│       = 367.35                     │
│  points = [9000, 9367, ..., 27000] │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Calculate P&L for Each Point      │
│  (Same formula, different range)   │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Update Chart:                     │
│  - X-axis expands to 9K-27K        │
│  - Y-axis adjusts automatically    │
│  - Payoff curve extends            │
│  - Smooth transition animation     │
└────────────────────────────────────┘
```

---

## Flow 7: Save Strategy

```
┌────────────────────────────┐
│  User clicks "Save         │
│  Strategy" button          │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  onClick Handler:              │
│  setIsSaving(true)             │  ← Show saving state
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Prepare Request:                  │
│  {                                 │
│    name: "Covered Call",           │
│    type: "covered-call",           │
│    entryDate: "2025-12-26",        │
│    expiryDate: "2026-01-26",       │
│    parameters: {...},              │
│    notes: "...",                   │
│    timestamp: "2025-12-26T..."     │
│  }                                 │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  API Call:                     │
│  saveStrategyToBackend(req)    │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  POST /api/strategies          │
│  Content-Type: application/json│
│  Body: { ... }                 │
└──────────┬─────────────────────┘
           │
      ┌────┴─────┐
      │          │
      ▼          ▼
  ┌────────┐  ┌──────────┐
  │ 200 OK │  │ ERROR    │
  └───┬────┘  └────┬─────┘
      │            │
      ▼            ▼
┌──────────────┐  ┌────────────────┐
│ Response:    │  │ Catch error    │
│ {            │  │ Show toast     │
│   success:   │  │ "Save failed"  │
│   true,      │  │                │
│   id: "...", │  └────────────────┘
│   message:   │
│   "Saved"    │
│ }            │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│  Update UI:                  │
│  - setIsSaving(false)        │
│  - setShowSaveSuccess(true)  │
│  - Show toast: "Saved!"      │
│  - Auto-hide after 3 seconds │
└──────────────────────────────┘
```

---

## Flow 8: Error Handling

```
┌────────────────────────────┐
│  API Call Fails            │
│  (Network error, timeout,  │
│   400/500 response)        │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  try-catch Block:              │
│  catch (error) {               │
│    console.error(error)        │
│    // Keep existing data       │
│  }                             │
└──────────┬─────────────────────┘
           │
      ┌────┴─────┐
      │          │
      ▼          ▼
┌──────────────────┐  ┌────────────────────┐
│ Payoff Calc Fail │  │ Save Strategy Fail │
└──────┬───────────┘  └────────┬───────────┘
       │                       │
       ▼                       ▼
┌──────────────────┐  ┌────────────────────┐
│ Keep existing    │  │ Show error toast:  │
│ chart data       │  │ "Failed to save.   │
│ No blank screen  │  │  Please try again."│
└──────────────────┘  └────────────────────┘
       │                       │
       └───────────┬───────────┘
                   │
                   ▼
          ┌────────────────┐
          │ User notified  │
          │ App continues  │
          │ working        │
          └────────────────┘
```

---

## Flow 9: Mobile Responsive Behavior

```
┌────────────────────────────┐
│  Window Width < 768px      │
│  (Mobile Device)           │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  Initial State:                │
│  - showMobileDetail = false    │
│  - Sidebar visible (full width)│
│  - Detail panel hidden         │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────┐
│  User Selects Strategy     │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  setShowMobileDetail(true)     │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  CSS Classes Update:           │
│  - Sidebar: add "hidden" class │
│  - Detail: remove "hidden"     │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  UI Transition:                │
│  - Sidebar slides out (left)   │
│  - Detail slides in (right)    │
│  - Back button appears         │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────┐
│  User Clicks Back Button   │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  setShowMobileDetail(false)    │
└──────────┬─────────────────────┘
           │
           ▼
┌────────────────────────────────┐
│  UI Transition:                │
│  - Detail slides out (right)   │
│  - Sidebar slides in (left)    │
│  - Back to strategy list       │
└────────────────────────────────┘
```

---

## Component Communication Map

```
                    ┌─────────────┐
                    │   App.tsx   │
                    └──────┬──────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
    ┌────────────┐  ┌──────────┐  ┌─────────────┐
    │  TopNav    │  │ Sidebar  │  │ DetailPanel │
    └────────────┘  └─────┬────┘  └──────┬──────┘
                          │               │
         ┌────────────────┼───────────────┼────────────┐
         │                │               │            │
         ▼                ▼               ▼            ▼
  ┌─────────────┐  ┌────────────┐  ┌──────────┐  ┌─────────┐
  │ StrategyNode│  │ContextMenu │  │ Payoff   │  │ Custom  │
  │  (Card)     │  │            │  │ Diagram  │  │ Builder │
  └─────────────┘  └────────────┘  └────┬─────┘  └─────────┘
                                        │
                                        ▼
                                ┌────────────────┐
                                │  payoffApi.ts  │
                                └────────┬───────┘
                                         │
                        ┌────────────────┴───────────────┐
                        │                                │
                        ▼                                ▼
                ┌───────────────┐              ┌─────────────────┐
                │ Backend API   │              │ localCalcs.ts   │
                │ (Node/Express)│              │ (Fallback)      │
                └───────────────┘              └─────────────────┘
```

---

## State Flow Diagram

```
User Action → Event Handler → State Update → useEffect → Side Effect
                                    │                         │
                                    ▼                         ▼
                              Re-render ←─────────────── API Call
                                    │                         │
                                    ▼                         ▼
                            Update DOM ←────────────── Update State
                                    │
                                    ▼
                            User sees change

Example:
Click "Save" → onClick → setIsSaving(true) → useEffect → saveToBackend()
                              │                               │
                              ▼                               ▼
                         Show spinner ←─────────── Response received
                              │                               │
                              ▼                               ▼
                    Disable button ←──────── setIsSaving(false)
                              │                               │
                              ▼                               ▼
                     User sees feedback ←─── Show success toast
```

---

## Request/Response Timing Diagram

```
Time
 0ms ┌─── User clicks "Calculate" ───┐
     │                               │
10ms │  setIsLoadingPayoff(true)     │
     │  ├─ Show spinner              │
     │  └─ Disable inputs            │
     │                               │
15ms │  fetchPayoffData(request)     │
     │  └─ Check backend health      │
     │                               │
20ms │     GET /api/health           │
     │     ├─ Timeout: 2000ms        │
     │     │                         │
120ms│     └─ Response: 200 OK       │ ← Backend available
     │                               │
125ms│  POST /api/calculate-payoff   │
     │  ├─ Send strategy params      │
     │  │                            │
     │  │  (Backend Processing)      │
     │  │  - Validate input          │
     │  │  - Calculate payoff        │
     │  │  - Generate 50 points      │
     │  │                            │
250ms│  └─ Response: 200 OK          │
     │     └─ Payoff data array      │
     │                               │
255ms│  setPayoffData(data)          │
     │  setIsLoadingPayoff(false)    │
     │  ├─ Hide spinner              │
     │  └─ Enable inputs             │
     │                               │
260ms│  Chart re-renders             │
     │  ├─ Animate transition (500ms)│
     │  │                            │
760ms│  └─ Animation complete        │
     │                               │
     └─── User sees updated chart ───┘
```

---

## Responsive Breakpoint Flow

```
Window Width Change
       │
       ▼
┌──────────────┐
│ Check width  │
└──────┬───────┘
       │
   ┌───┴────┬────────────┬──────────┐
   │        │            │          │
   ▼        ▼            ▼          ▼
┌─────┐ ┌──────┐  ┌─────────┐  ┌────────┐
│ 0-  │ │ 641- │  │ 1025-   │  │ 1280+  │
│640px│ │1024px│  │ 1279px  │  │        │
└──┬──┘ └───┬──┘  └────┬────┘  └───┬────┘
   │        │          │           │
   │        │          │           │
   ▼        ▼          ▼           ▼
Mobile   Tablet    Desktop      Wide
   │        │          │           │
   ▼        ▼          ▼           ▼
Stack    2-Col      2-Col       2-Col
Vertical Layout    Optimized   Spacious
   │        │          │           │
   ▼        ▼          ▼           ▼
Sidebar  Sidebar    Sidebar     Sidebar
Hidden  256px      320px       320px
OR Full  Fixed      Fixed       Fixed
Width
   │        │          │           │
   ▼        ▼          ▼           ▼
Detail   Detail     Detail      Detail
Full     Flex-1     Flex-1      Max-w-7xl
Width               Centered
```

---

## Summary: Key Integration Points

### 1. **User → State**
- Event handlers (`onChange`, `onClick`)
- State updaters (`setParams`, `setPayoffData`)

### 2. **State → Effect**
- `useEffect` hooks watching state changes
- Triggers API calls or recalculations

### 3. **Effect → API**
- `fetchPayoffData()` in `payoffApi.ts`
- Health check → Backend or local fallback

### 4. **API → Backend**
- HTTP POST to `/api/calculate-payoff`
- Request validation and processing

### 5. **Backend → Response**
- JSON array of `{ price, pnl }` objects
- Status codes (200, 400, 500)

### 6. **Response → State**
- `setPayoffData(response)`
- Update derived state (break-even, max/min)

### 7. **State → UI**
- React re-renders components
- Recharts animates chart updates

### 8. **UI → User**
- Visual feedback (spinner, toast, chart)
- Smooth transitions and animations

---

**Last Updated:** December 26, 2025
